import styled from "@emotion/styled";

export const Carriers = styled.p`
  color: var(--text);
  font-size: 13px;
  line-height: 1.3;
  margin: 0;

  span {
    color: var(--text-h);
    font-weight: 700;
  }
`;
